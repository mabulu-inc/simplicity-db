{
  "type": "module",
  "name": "@mabulu-inc/db",
  "version": "2.3.1",
  "description": "simple postgresql pooled client",
  "main": "index.mjs",
  "scripts": {
    "lint": "eslint .",
    "release": "npm i && npm run lint && npm test && standard-version && git push --follow-tags && npm publish",
    "test": "node --test"
  },
  "author": "",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/mabulu-inc/db.git"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.3.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.1.0",
    "globals": "^17.4.0",
    "standard-version": "^9.5.0"
  }
}
